import { BlockMapType } from "react-notion";

export type ProjectType = {
  id: string;
  Name: string;
  description: string;
  link: string;
  repo: string;
  framework: string;
  language: string;
};
